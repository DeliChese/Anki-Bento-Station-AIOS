# ═══════════════════════════════════════════════════════════
# Bento Station AIOS — .gitignore (repo PUBLIC)
# Loại tài liệu nội bộ (AI-guide / cấu trúc) + dữ liệu nhạy cảm
# ═══════════════════════════════════════════════════════════

# ── Dữ liệu cá nhân / cấu hình profile (chứa API key, ảnh) ──
user_files/
.git.bak/

# ── Python ──
__pycache__/
*.py[cod]
.pytest_cache/

# ── OS ──
.DS_Store
Thumbs.db
Desktop.ini

# ── Logs ──
*.log
